
# crear una virtual env donde vas a clonar el repositorio
# usar python 3.11 para crear el entorno virtual

# Crear el entorno virtual
python3.11 -m venv venv

# Activar entorno virtual (recomendado)
source venv/bin/activate


# Clonar del repositorio el proyecto: https://github.com/Ivan-Gongora/SistemaIoT-Analisis.git


# Instalar todas las dependencias desde requirements.txt
# Nota: verificar que estes en la carpeta donde esta el requirements.txt para que funcione la instalacion de los requerimientos
pip install -r requirements.txt
pip install -r requerimients2.txt

# Acceder a la carpeta y en la misma altura de las carpetas de app,vue,web
# Ejecutar lo siguiente para levantar el fastapi

uvicorn app.principal:aplicacion --reload --port 8001

Nota: tener libre el puerto 8001 porque es importante para que funcione con el bakcend